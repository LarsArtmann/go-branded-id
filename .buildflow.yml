# BuildFlow Configuration
# See: https://github.com/larsartmann/buildflow
# Disable go-auto-upgrade: it rewrites encoding/json v1->v2, which corrupts
# the dual-mode JSON files (id_json_v1.go, json_helpers_v1_test.go) on every
# commit. This library deliberately supports BOTH v1 and v2 via build tags.
skip_steps:
  - go-auto-upgrade
